# [마중365] 출소자 재사회화 온보딩 네비게이터

KDT 해커톤 프로젝트 (팀 '여름아 부탁해', 5인). **예선은 끝났고 지금은 본선(앱) 개발이다.** 예선 코드는 5탭 네비게이션·무저장·익명 전제였고, 본선은 단일 화면 + 아코디언·암호화 저장·기기 세션으로 간다. **정보 구조가 근본적으로 다르므로 예선 전제를 그대로 이어받지 않는다.**

## 구조

| 폴더 | 내용 | 규칙 문서 |
|---|---|---|
| `Majung-Frontend/` | Expo(React Native) + expo-router + NativeWind | `Majung-Frontend/AGENTS.md` |
| `Majung-Backend/` | FastAPI (Python 3.12+, uv) | `Majung-Backend/AGENTS.md` |
| `_bmad-output/specs/spec-majung-2nd/` | **본선 정본 계약** | — |
| `_bmad-output/specs/spec-majung-demo/` | 예선 계약 (이력) | — |
| `tools/` | RAG 수집기, 기관 데이터 생성 스크립트 | — |
| `_bmad/`, `.codex/skills/` | BMad-Method 프레임워크 | — |

## 작업 시작 전 필독

1. **`majung365_리뉴얼_개발플로_기획안_v2.md`** — 제품 결정의 정본. **레포 밖(`C:\Users\skwog\Downloads\`)에 있다.** 화면·정책·문구가 전부 이 문서의 절을 가리킨다
2. **`_bmad-output/specs/spec-majung-2nd/`** — 개발 계약
   - `intake-contract.md` — 용어(가입·초기 진단·분야·문항·지원 항목·할 일), 6분야 ↔ 지원 항목 14개
   - `intake-questions.md` — 초기 진단 문항 세트 (필수 14 + 꼬리 13)
   - `route-contacts.md` — 지원 항목별 연락처. 사람이 확인한 표이며 본문 자동 추출 금지
   - `graph-design.md` — 그래프 데이터 설계
   - `SSOT.md` — 상위 계약. **충돌 시 날짜가 늦은 결정이 이긴다**
3. **예선 계약(`spec-majung-demo/`)은 이력이다.** 같은 사안에서 충돌하면 본선 결정을 따른다

## 전역 불변 규칙

### 🔒 0. 보안이 1번이다 — "첫째도 보안, 둘째도 보안"

출소자 대상 서비스: **출소 사실 자체가 극도로 민감한 개인정보**이며, 유출은 사용자의 사회 복귀를 파괴한다. 편의성과 보안이 충돌하면 항상 보안을 택한다.

> **본선에서 전제가 하나 바뀌었다.** 저장하지 않을 때는 유출될 데이터 자체가 없었다. **저장하는 순간부터 "출소자 명단"이 실재한다.** 아래 보호 설계는 선택이 아니라 저장 결정에 딸린 필수 조건이다 (기획서 §3.1·§9).

1. **데이터 최소화** — 목적에 필요한 최소만 수집·저장한다. 근거 없는 수집 금지. **주민등록번호를 비롯한 고유식별정보는 받지 않는다**
2. **저장하는 것은 암호화한다** (§9.2). 이름·생일·출소날짜 조합은 개인을 특정하므로 **조합 단위로 보호 수준을 정한다**
3. **죄목은 민감정보에 준해 처리한다** — 별도 동의를 받고 별도 테이블에 둔다. 동의를 철회하면 그 항목만 파기한다 (§9.5·§3.4)
4. **대화 내용 = 민감정보** — 서버 로그·에러 리포트에 사용자 입력 원문 금지. **LLM 전송 전 마스킹을 거친다** (§9.3)
5. **보이지 않는 답은 보내지 않는다** — 화면에서 사라진 답은 사용자가 철회한 것이다. 전송·저장 대상에서 뺀다 (§3.8)
6. **위치 좌표를 서버로 보내지 않는다** — 기기에서 시군구로 바꾸고 좌표는 버린다. Google Maps Geocoding API를 쓰지 않는다 (§5.4)
7. **비밀 커밋 금지** — `.env`만. 클라이언트 번들에 어떤 비밀도 넣지 않는다
8. **서드파티 추가는 사용자 확인 필수** — 의존성 하나마다 "이게 어떤 데이터를 어디로 보내나"를 검토한다
9. **보관 기간은 마지막 접속일로부터 1년** (§9.4). 사용자가 열람·수정·삭제할 수 있는 경로를 둔다. 삭제는 즉시 처리한다
10. **public 전환 전 보안 검토 필수** — 히스토리 포함

### 기타 불변 규칙

- 서비스명 표기: **마중365**
- Claude API 키는 백엔드에만 — 프론트 번들에 어떤 비밀도 넣지 않는다
- **분류 단위는 지원 항목(route)이다.** 폐기된 6영역(`Area`) 대신 `RouteId`(R1~R4·R6~R15)를 쓴다. **R5는 결번이며 번호를 다시 매기지 않는다**
- 카피는 A 쐐기(공단 시설 내 온보딩) 프레임 — "출소 그다음 날부터 앱을 켠다"류 B 프레임 문구 금지
- 큰 글씨 (저리터러시 사용자 전제). 화면 문구의 어체는 `humanize-korean` 스킬로 점검해 확정한다
- 화면에 **"죄목"·"설문"·"영역"**을 쓰지 않는다. 각각 "어떤 일로 계셨는지"·"상황 알아보기"·"분야"다
- 관리 노트·SSOT는 Obsidian `출소자 지원 서비스/` 폴더에 (레포 문서가 정본, Obsidian은 관리 요약)

## 예선에서 뒤집힌 것 (그대로 이어받지 말 것)

| 예선 | 본선 |
|---|---|
| 익명 전제 · 실명 수집 없음 | **이름·생일·출소날짜를 받아 암호화 저장한다** (§3.1) |
| 대화를 서버에 남기지 않음 | **AI 채팅 내역을 암호화 저장한다.** 남지 않으면 어제 받은 안내가 사라진다 (§6.3) |
| 계정·로그인 없음 | **가입 화면에서 계정을 만들고 기기 세션으로 자동 로그인한다** (§2.4) |
| DB 없음 · JSON 파일 | **본선은 DB가 필요하다.** 위 셋이 전부 저장을 전제한다 |
| 5탭 네비게이션 | **단일 화면 + 서류철 인덱스 탭 아코디언** (§5.1) |
| 지도 화면 (Google Maps) | **지도 없음.** 시군구 기반 기관 안내로 대체 (§5.4) |
| 음성 감정 인식 | **범위에서 제외.** 되살리지 않는다 |
| 진입 게이트(코드 입력) | **폐지.** QR은 스토어로 직접 간다 (§2.2·§2.3) |
| 완료 체크는 표시형 | **완료를 누르면 다음 탭이 열린다** (§5.2) |
