# [마중365] 출소자 재사회화 온보딩 네비게이터

KDT 해커톤 프로젝트 (팀 '여름아 부탁해', 5인). **예선은 끝났고 지금은 본선(앱) 개발이다.** 예선 코드는 무저장·익명 전제였고, 본선은 암호화 저장·기기 세션으로 간다. **정보 구조가 근본적으로 다르므로 예선 전제를 그대로 이어받지 않는다.**

> 화면 구조는 **하단 메뉴바 다섯 칸 + 홈 안의 서류철 인덱스 탭 아코디언**이다. 아코디언은 본선에서 새로 정한 것이고(§5.1), 메뉴바는 2026-08-26에 되살렸다 — 화면이 여섯 개로 늘어 이동 수단이 다시 필요해졌다 (`decision-log-2026-08-26.md` B-1).

## 구조

| 폴더 | 내용 | 규칙 문서 |
|---|---|---|
| `Majung-Frontend/` | Expo(React Native) + expo-router + NativeWind | `Majung-Frontend/CLAUDE.md` |
| `Majung-Backend/` | FastAPI (Python 3.12+, uv) | `Majung-Backend/CLAUDE.md` |
| `_bmad-output/specs/spec-majung-2nd/` | **본선 정본 계약** | — |
| `_bmad-output/specs/spec-majung-demo/` | 예선 계약 (이력) | — |
| `tools/` | RAG 수집기, 기관 데이터 생성 스크립트 | — |
| `_bmad/`, `.claude/skills/` | BMad-Method 프레임워크 | — |

## 작업 시작 전 필독

1. **`majung365_리뉴얼_개발플로_기획안_v2.md`** — 제품 결정의 정본. 화면·정책·문구가 전부 이 문서의 절을 가리킨다
2. **`_bmad-output/specs/spec-majung-2nd/`** — 개발 계약
   - `intake-contract.md` — 용어(가입·초기 진단·분야·문항·지원 항목·할 일), 6분야 ↔ 지원 항목 14개
   - `intake-questions.md` — 초기 진단 문항 세트 (필수 14 + 꼬리 13)
   - `route-contacts.md` — 지원 항목별 연락처. 사람이 확인한 표이며 본문 자동 추출 금지
   - `copy-voice.md` — **화면 문구 어체 규칙.** 화면에 나갈 말을 쓰기 전에 읽는다
   - `graph-design.md` — 그래프 데이터 설계
   - `SSOT.md` — 상위 계약. **충돌 시 날짜가 늦은 결정이 이긴다**
3. **예선 계약(`spec-majung-demo/`)은 이력이다.** 같은 사안에서 충돌하면 본선 결정을 따른다

## 전역 불변 규칙

### 🔒 0. 보안이 1번이다 — "첫째도 보안, 둘째도 보안"

출소자 대상 서비스: **출소 사실 자체가 극도로 민감한 개인정보**이며, 유출은 사용자의 사회 복귀를 파괴한다. 편의성과 보안이 충돌하면 항상 보안을 택한다.

> **본선에서 전제가 하나 바뀌었다.** 저장하지 않을 때는 유출될 데이터 자체가 없었다. **저장하는 순간부터 "출소자 명단"이 실재한다.** 아래 보호 설계는 선택이 아니라 저장 결정에 딸린 필수 조건이다 (기획서 §3.1·§9).

1. **데이터 최소화** — 목적에 필요한 최소만 수집·저장한다. 근거 없는 수집 금지. **주민등록번호를 비롯한 고유식별정보는 받지 않는다**
2. **저장하는 것은 암호화한다** (§9.2). 이름·생일·출소날짜 조합은 개인을 특정하므로 **조합 단위로 보호 수준을 정한다**
3. **죄목은 민감정보에 준해 처리한다** — 별도 동의를 받고 별도 테이블에 둔다. 동의를 철회하면 그 항목만 파기한다 (§9.5·§3.4)
4. **대화 내용 = 민감정보** — 서버 로그·에러 리포트에 사용자 입력 원문 금지. **LLM 전송 전 마스킹을 거친다** (§9.3)
5. **보이지 않는 답은 보내지 않는다** — 화면에서 사라진 답은 사용자가 철회한 것이다. 전송·저장 대상에서 뺀다 (§3.8)
6. **위치 좌표를 받고 저장한다** (2026-08-26 결정 F-1로 **뒤집힘**). 예선부터 이어온 "좌표를 우리 서버로 보내지 않는다"는 더 이상 유효하지 않다
   - **뒤집은 이유**: 시군구까지만 아는 서버는 그 동네 기관들의 한가운데로 거리를 쟀는데, **시군구 안에서 그 한가운데가 엉뚱한 곳을 가리켰다.** 군포시는 기관이 산본신도시에 몰려 있어, 군포역에 사는 사람에게 산본 주민센터가 먼저 나왔다
   - 사용자 판단이며 근거는 출소자 인터뷰다 — 유출 위험이 어느 정도 있어도 편의성이 보장되면 괜찮다는 응답이었다. **결정 책임은 사용자에게 있다**
   - **남기는 것은 마지막 한 자리뿐이다.** `app_user.place_enc`에 암호화해 덮어쓴다. 지나온 자리를 쌓으면 동선 기록이 되는데 그것은 승인된 범위가 아니다
   - 동으로 바꾸는 계산은 그대로 기기에서 한다 (`src/shared/location/`). 안드로이드 `Geocoder`와 웹 미지원 문제는 이 결정과 무관하게 그대로다
   - 기관 주소를 좌표로 바꾸는 것(`tools/fill_coordinates.py`)은 이 규칙과 무관하다. **기관 주소는 개인정보가 아니다**
   - **여전히 유효한 것**: 좌표는 대한민국 범위 밖이면 받지 않고, 조회 조건을 로그에 남기지 않는다
7. **비밀 커밋 금지** — `.env`만. 클라이언트 번들에 어떤 비밀도 넣지 않는다
8. **서드파티 추가는 사용자 확인 필수** — 의존성 하나마다 "이게 어떤 데이터를 어디로 보내나"를 검토한다
9. **보관 기간은 마지막 접속일로부터 1년** (§9.4). 사용자가 열람·수정·삭제할 수 있는 경로를 둔다. 삭제는 즉시 처리한다
10. **public 전환 전 보안 검토 필수** — 히스토리 포함

### 기타 불변 규칙

- 서비스명 표기: **마중365**
- Claude API 키는 백엔드에만 — 프론트 번들에 어떤 비밀도 넣지 않는다
- **분류 단위는 지원 항목(route)이다.** 폐기된 6영역(`Area`) 대신 `RouteId`(R1~R4·R6~R15)를 쓴다. **R5는 결번이며 번호를 다시 매기지 않는다**
- 카피는 A 쐐기(공단 시설 내 온보딩) 프레임 — "출소 그다음 날부터 앱을 켠다"류 B 프레임 문구 금지
- 큰 글씨 (저리터러시 사용자 전제)
- **화면 문구의 어체는 `spec-majung-2nd/copy-voice.md`가 정한다** (2026-08-31 결정). 디자이너 시안의 **합니다체 고지투**를 정본으로 채택했고, 그전까지 쓰던 해요체 대화투를 뒤집었다
  - 네 갈래다 — 제목·라벨은 명사형, 시스템이 하는 일은 합니다체, 사용자가 해야 할 일은 "~해 주세요", 사용자가 할 수 있는 일과 안심시키는 말은 해요체
  - `humanize-korean` 스킬은 **AI 티를 걷어내는 용도로만** 쓴다. 어체 판정은 그 스킬이 아니라 위 문서가 한다
- 화면에 **"영역"**을 쓰지 않는다. "분야"다. 폐기한 6영역 분류와 혼동된다
- 화면에 **"죄목"**을 쓰지 않는다. 그 말이 화면에 뜨는 것 자체가 낙인이다
  - **다만 "수용 사유"는 쓴다** (2026-08-31에 시안을 따라 허용). 그전에는 "어떤 일로 계셨나요"였다
  - **"설문"은 2026-08-26에 풀었다** (`decision-log-2026-08-26.md` A-5). 조사받는 어감을 피하려 막아 두었으나, 사용자에게는 오히려 익숙한 말이라는 판단이다
- 관리 노트·SSOT는 Obsidian `출소자 지원 서비스/` 폴더에 (레포 문서가 정본, Obsidian은 관리 요약)

## 예선에서 뒤집힌 것 (그대로 이어받지 말 것)

| 예선 | 본선 |
|---|---|
| 익명 전제 · 실명 수집 없음 | **이름·생일·출소날짜를 받아 암호화 저장한다** (§3.1) |
| 대화를 서버에 남기지 않음 | **AI 채팅 내역을 암호화 저장한다.** 남지 않으면 어제 받은 안내가 사라진다 (§6.3) |
| 계정·로그인 없음 | **가입 화면에서 계정을 만들고 기기 세션으로 자동 로그인한다** (§2.4) |
| DB 없음 · JSON 파일 | **본선은 DB가 필요하다.** 위 셋이 전부 저장을 전제한다 |
| 5탭 네비게이션 | 단일 화면 + 서류철 인덱스 탭 아코디언 (§5.1) → **2026-08-26 되돌림.** 하단 메뉴바 다섯 칸(홈·알림·상담·지도·내 정보)을 되살렸다. **아코디언은 그대로이며 홈 칸 안에 있다** (`decision-log-2026-08-26.md` B-1) |
| 지도 화면 (Google Maps) | 지도 없음 (§5.4) → **2026-08-26 되돌림.** 구글 지도를 넣는다 (B-2). **좌표를 우리 서버가 받고 저장하는 것도 같은 날 뒤집었다** (F-1) |
| 음성 감정 인식 | **범위에서 제외.** 되살리지 않는다 |
| 진입 게이트(코드 입력) | **폐지.** QR은 스토어로 직접 간다 (§2.2·§2.3) |
| 완료 체크는 표시형 | **완료를 누르면 다음 탭이 열린다** (§5.2) |
