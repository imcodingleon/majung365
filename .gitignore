# ── 비밀 (절대 커밋 금지 — 커밋 #1부터) ──
.env
.env.*
!.env.example

# ── Node / Expo ──
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# ── Python ──
__pycache__/
.venv/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/

# ── 로컬 도구 상태 (기계별) ──
.claude/settings.local.json
.omc/
*.log

# ── OS ──
.DS_Store
Thumbs.db
*.pem
*.pem.pub
ser-lab/
.playwright-mcp/

.vercel
.env*

# ── 작업 부산물 (여러 세션이 한 폴더를 쓰면서 루트에 쌓인 것) ──
#
# **`/`로 시작해 루트만 막는다.** 안 그러면 `assets/images/region-banner.png` 같은
# 코드 자산까지 함께 사라진다.
.agents/
_workspace/
tools/rag-crawler/output/
# **미검수 법률 초안을 레포에 두지 않는다.** rag-crawler와 반대로 가는 이유는,
# 검수를 거치지 않은 조문 원문이 레포에 있으면 언젠가 누가 그것을 읽는 코드를 쓰기 때문이다.
tools/legal-constraints/output/
questions-review.md
r2.txt
/*.png
/*.svg
/*.docx
/majung365_simple_architecture.html
