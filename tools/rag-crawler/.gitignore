.venv/
__pycache__/

# 개별 수집 문서는 재수집으로 만들 수 있으므로 올리지 않는다.
output/*/

# 다만 묶음 파일은 올린다. KB의 source_urls가 이 문서들을 가리키므로
# 레포에 없으면 "이 안내가 어디서 왔나"를 나중에 확인할 수 없다.
# 차단된 페이지는 재수집해도 복원되지 않고, 원본이 바뀌면 옛 내용은 못 살린다.
!output/documents.jsonl
