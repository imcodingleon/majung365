import { AnalyzingScreen } from "@/features/onboarding";

// 분석 로딩 화면 — 온보딩 완료 직후, 오늘의 과제 계산(C6+C7) 대기. 구현은 features/onboarding.
export default function AnalyzingRoute() {
  return <AnalyzingScreen />;
}
