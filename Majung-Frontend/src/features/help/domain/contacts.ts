// 상시 도움 연결 (§5.3). 이 번호들은 확정이며 별도 검증 절차를 두지 않는다.
// 번호만 나열하면 무엇을 눌러야 할지 또 판단해야 하므로, 무엇을 위한 번호인지 쉬운 말로 함께 적는다.

export type HelpLine = {
  /** 사용자에게 보이는 번호. */
  label: string;
  /** 실제 발신에 쓸 숫자만 남긴 번호. */
  dial: string;
  /**
   * 무엇을 하는 번호인지.
   *
   * **2026-08-31에 성격이 바뀌었다.** 전에는 "마음이 많이 힘들 때"처럼 **거는 사람의
   * 상황**을 적었고, 지금은 "정신건강 위기 상담"처럼 **받는 쪽이 하는 일**을 적는다.
   * 시안을 따른 것이다. 필드 이름은 `when`으로 두었지만 담는 것은 상황이 아니다.
   */
  when: string;
  /** 기관 이름. 번호만 있으면 어디에 거는지 알 수 없다. */
  org: string;
};

/** 상담 번호. 무엇을 말해야 하는지 묻지 않는다. */
export const COUNSEL_LINES: readonly HelpLine[] = [
  { label: "109", dial: "109", when: "정신건강 위기 상담", org: "자살예방 상담 · 24시간" },
  { label: "129", dial: "129", when: "긴급 생계 · 복지 상담", org: "보건복지상담센터" },
  { label: "1342", dial: "1342", when: "약물 관련 상담", org: "24시간" },
  {
    label: "1670-7004",
    dial: "16707004",
    when: "지원 신청 및 일반 상담",
    org: "한국법무보호복지공단",
  },
];

/** 긴급신고. 상담과 성격이 다르므로 화면에서도 영역을 나눈다. */
export const EMERGENCY_LINES: readonly HelpLine[] = [
  { label: "112", dial: "112", when: "범죄 · 위협 상황 신고", org: "경찰" },
  { label: "119", dial: "119", when: "화재 · 응급구조 신고", org: "소방 · 구급" },
];
