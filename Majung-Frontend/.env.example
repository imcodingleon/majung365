# 실제 값은 .env에 둔다 (git 추적 안 됨). 이 파일은 구조 공유용이다.
#
# ⚠️ **EXPO_PUBLIC_ 접두사는 클라이언트 번들에 그대로 박힌다.** 비밀은 여기 두지 않는다.
# 번들을 열면 값이 보이므로 "노출되어도 되는 것"만 쓴다 (CLAUDE.md 규칙 7).

# 백엔드 주소. 로컬 개발은 http://localhost:8000
EXPO_PUBLIC_API_URL=https://3-34-251-223.sslip.io
